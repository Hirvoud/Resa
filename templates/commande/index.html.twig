{% extends 'base.html.twig' %}

{% block body %}

    <div class="col-lg-12">
        <h1>Billetterie du Musée du Louvre</h1>
    </div>

    <div class="col-lg-6">
        <h2>Bienvenue</h2>
    </div>

    <div class="col-lg-6">
        <h2>Liste des tarifs</h2>
    </div>

    <div class="col-lg-6">
        {{ form_start(form) }}

            {# refaire le formulaire élément par élément #}
            {{ form_widget(form.dateVisite, {"id":"datepicker"}) }}
            {{ form_rest(form) }}
        {{ form_end(form) }}
    </div>



{% endblock %}
